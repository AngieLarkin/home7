---
version: "3.8"
services:
  tomcat:
    image: tomcat
    ports:
      - "8080:8080"
    volumes:
      - type: volume
        source: vol7
        target: /mnt

    depends_on:
      - maven

  maven:
    build: maven-custom
    volumes:
      - type: volume
        source: vol7
        target: /root/boxfuse-sample-java-war-hello/target

volumes:
  vol7:
